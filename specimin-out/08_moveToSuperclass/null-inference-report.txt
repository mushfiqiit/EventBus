### A) Annotation decisions

Here are the annotation decisions for the provided code:

| location | inferred annotation | one-sentence reason |
| --- | --- | --- |
| `clazz` field | `@Nullable` | Flagged by NullAway as potentially null in the warnings. |
| `skipSuperClasses` field | (no annotation) | Guaranteed non-null by default under NullAway. |
| `moveToSuperclass()` return type | (no annotation) | The method is void, so it doesn't have a return type. |
| `clazz.getSuperclass()` expression | `@Nullable` | The `getSuperclass()` method returns null if the class is not a subclass of any other class. |
| `clazz.getName()` expression | `@Nullable` | The `getName()` method does not return null, but since `clazz` is nullable, the expression is also nullable. |

### B) Unsafe dereferences

Here are the dereferences of a `@Nullable` value in the code that could throw a `NullPointerException`:

1. `clazz.getSuperclass()` (line 13): This expression is flagged by NullAway as potentially null.
2. `clazz.getName()` (line 16): Although `getName()` itself does not return null, `clazz` is nullable, so this expression could also throw a `NullPointerException`.

### C) Corrected annotated code

```java
// === org/greenrobot/eventbus/SubscriberMethodFinder.java ===
package org.greenrobot.eventbus;

import javax.annotation.Nullable;

class SubscriberMethodFinder {

    static class FindState {

        @Nullable
        Class<?> clazz;

        boolean skipSuperClasses;

        void moveToSuperclass() {
            if (skipSuperClasses) {
                clazz = null;
            } else {
                clazz = clazz == null ? null : clazz.getSuperclass();
                if (clazz != null) {
                    String clazzName = clazz.getName();
                    if (clazzName.startsWith("java.") || clazzName.startsWith("javax.") || clazzName.startsWith("android.") || clazzName.startsWith("androidx.")) {
                        clazz = null;
                    }
                }
            }
        }
    }
}

// === org/jspecify/annotations/NullUnmarked.java ===
// This file remains unchanged
package org.jspecify.annotations;

import java.lang.annotation.Documented;
import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;

@Documented
@Retention(RetentionPolicy.RUNTIME)
@Target({ ElementType.METHOD })
public @interface NullUnmarked {
}
```